Read analyze.py, optimize.py and rtlai/ first, then write a concise project brief covering: what the tool does, the module layout, and these hard rules — never hardcode design-specific knowledge into prompts or code (it must work on arbitrary RTL); never weaken a formal claim (bounded results stay labelled bounded, with the depth); generated RTL is Verilog-2001 only; run python3 -c "import optimize" after any Python edit. Note that rtlai/formal.py, rtlai/optimizer/decide.py and the payload RTL in designs/ are validated — do not modify them unless I explicitly ask.
webapp/app.py streams optimize.py output over SSE. Create webapp/templates/index.html: dark technical UI, six-stage pipeline tracker driven by parsing the live log, colour-coded terminal, collapsible panel for the planner's reasoning, and a results section with metric cards, a formal-verdict table distinguishing proven / bounded / not-equivalent, and download links for the optimized RTL. Then run the app and fix whatever breaks.
